---
import { Image } from "astro:assets";
import LuisIBridge from "~/assets/bridge.jpeg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={LuisIBridge}
  widths={widths}
  sizes={sizes}
  alt="Luis I Bridge in Porto, Portugal"
/>
